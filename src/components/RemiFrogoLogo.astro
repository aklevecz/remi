---
const { default: innerHTML } = await import(`./svgs/remifrogologo.svg?raw`);
---
<Fragment data-remi-logo set:html={innerHTML}/>

<style is:global>
  svg[data-remi-logo] {
    width:100%;
    height:100%;
    max-width: 700px;
    display: block;
    margin: 0 auto;
  }
  svg[data-remi-logo] path {
    transition: fill 0.3s;
    fill: var(--secondary-color);
  }
  svg[data-remi-logo] path.white {
    fill: white;
  }
</style>