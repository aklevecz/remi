---
const { default: innerHTML } = await import(`./svgs/tattoo-logo.svg?raw`);
---
<Fragment data-remobotron-logo set:html={innerHTML}/>

<style is:global>
  svg[data-remobotron-logo] {
    width:100%;
    height:100%;
    max-width: 700px;
    display: block;
    margin: 0 auto;
  }
  svg[data-remobotron-logo] path {
    transition: fill 0.3s;
    fill: var(--secondary-color);
  }
  svg[data-remobotron-logo] path.white {
    fill: white;
  }
</style>